import styled from 'styled-components';

const StyledTaskList = styled.div`
  color: #fff;
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  overflow-y: auto;
  padding: 10px;
`;

export default StyledTaskList;